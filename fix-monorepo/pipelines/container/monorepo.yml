---
include:
  - remote: "https://jobs.just-ci.dev/fix-monorepo/container/kaniko.yml"
  - remote: "https://jobs.just-ci.dev/fix-monorepo/container/grype.yml"

# skips default polyrepo job inherited
kaniko:
  rules:
    - when: never

# skips default polyrepo job inherited
grype:
  rules:
    - when: never
