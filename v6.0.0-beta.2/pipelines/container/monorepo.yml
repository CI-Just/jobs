---
include:
  - remote: "https://jobs.just-ci.dev/v6.0.0-beta.2/container/kaniko.yml"
  - remote: "https://jobs.just-ci.dev/v6.0.0-beta.2/container/grype.yml"

# skips default polyrepo job inherited
kaniko:
  rules:
    - when: never

# skips default polyrepo job inherited
grype:
  rules:
    - when: never
